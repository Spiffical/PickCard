characters = ["PICARD", "DATA"]

# this could be a class in the future so that other functions can rely on it
datamodel = {
    "type": "",
    "character": None,
    "body": ""
}

datatypes = ["heading", "action", "dialogue", "transition"]